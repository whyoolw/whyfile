Config(
 x: Fraction(0.5),
  y: Fraction(0.3),
  width: Absolute(700),
  height: Absolute(0),
  hide_icons: false, 
  ignore_exclusive_zones: true, 
  layer: Overlay, 
  hide_plugin_info: false, 
  close_on_click: true,
  show_results_immediately: false,

  plugins: [
    "libapplications.so",
    "librink.so",
    "libshell.so",
    "libdictionary.so",
    "libstdin.so",
    "libwebsearch.so",
  ],
)
